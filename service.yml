apiVersion: v1
kind: Service
metadata:
  name: jenkins-python-app-service
spec:
  type: NodePort
  selector:
    app: jenkins-python-app
  ports:
    - port: 90
      targetPort: 5190
      # Optional field
      nodePort: 30090
