apiVersion: v1
kind: Service
metadata:
  name: jenkins-python-app-service
spec:
  type: NodePort
  selector:
    app: jenkins-python-app
  ports:
    - port: 82
      targetPort: 5174
      # Optional field
      nodePort: 30011
